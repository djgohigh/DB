1. 프로젝트명 : JAVA-DB연동 및 CRUD 구현 개인프로젝트
2. 주제 : PC견적기능 구현(DANAWA)
3. 내용
   가. 사용자 로그인 이후 견적서 생성, 견적서 관리 기능 제공
   나. 견적서 생성
       1) CPU, MAINBOARD, VGA, RAM, SSD 순으로 보유부품 현황제공, 사용자는 현황에서 바로 선택 혹은 조건검색 후 선택가능
       2) 각 부품별 사용자가 선택한 데이터는 GET 메서드를 활용, 부품의 이름 및 가격을 추출하여 QUOTE(견적서) TABLE에 데이터 저장
   다. 견적서 관리
       1) 생성된 견적서 조회, 조회 이후 선택한 견적서에 대한 수정, 삭제기능 제공
       2) 견적서 수정 시 CPU, MAINBORAD, VGA, RAM, SSD 중 사용자가 수정을 원하는 것만 부품현황 제공해 선택한 데이터를 담음
       3) 선택한 부품 데이터의 이름 및 가격을 GET 메서드를 통해 추출하고, QUOTENAME(견적서명)을 SQL UPDATE~SET문의 WHERE에 활용함으로써 데이터를 UPDATE함
          * 사용자가 견적서관리에서 조회하고 싶은 견적서의 인덱스를 담는 정수값 QUOTECHOICE은 STATIC 전역변수로 선언 → 사용자가 최초 견적서를 선택하고 나서 데이터가 업데이트 될때까지 활용
